def smallest_largest(input_file, output_file):
    """
    Write an `output_file` that has the same contents as `input_file` with the following modifications:
        - First, subtract every integer in `input_file` by the smallest number its line in `input_file`.
        - Then, divide those integers by the largest number in its original line in `input_file`
    """
