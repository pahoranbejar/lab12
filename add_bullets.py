import sys


def add_bullets(input_file, output_file):
    """
    Writes an `output_file` that has the same contents as `input_file` but adds bullets "*" to each line of `input_file`
    """


add_bullets(sys.argv[1], sys.argv[2])